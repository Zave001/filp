# MusicalHub - Интернет-магазин музыкальных инструментов

Полнофункциональный онлайн-магазин музыкальных инструментов, разработанный с использованием Haskell (бэкенд) и React (фронтенд).

## 🚀 Возможности

### Бэкенд (Haskell)
- **REST API** с полным набором эндпоинтов
- **Аутентификация и авторизация** пользователей
- **Фильтрация и поиск** товаров
- **Система скидок** (комплекты, запасные струны)
- **PostgreSQL** база данных
- **Безопасное хеширование** паролей

### Фронтенд (React + Vite)
- **Современный UI** с адаптивным дизайном
- **Регистрация и вход** пользователей
- **Каталог товаров** с фильтрами и поиском
- **Корзина покупок** с расчетом стоимости
- **Оформление заказов** с автоматическими скидками

## 🛠 Технологии

### Бэкенд
- **Haskell** - основной язык
- **Scotty** - веб-фреймворк
- **PostgreSQL** - база данных
- **Aeson** - JSON обработка
- **Cryptonite** - шифрование

### Фронтенд
- **React 18** - UI библиотека
- **Vite** - сборщик и dev server
- **CSS Modules** - стилизация

## 📦 Установка и запуск

### Предварительные требования
- Haskell Stack
- Node.js и npm
- PostgreSQL

### Запуск бэкенда

1. Установите зависимости Haskell:
```bash
stack build
```

2. Создайте базу данных PostgreSQL и выполните миграции:
```bash
createdb musicalhub
psql -d musicalhub -f migrations/001-initial-schema.sql
psql -d musicalhub -f data/seed.sql
```

3. Запустите сервер:
```bash
stack run
```
Сервер будет доступен на `http://localhost:8080`

### Запуск фронтенда

1. Установите зависимости:
```bash
cd frontend
npm install
```

2. Запустите dev server:
```bash
npm run dev
```
Приложение будет доступно на `http://localhost:5173`

## 🔧 API Эндпоинты

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему

### Товары
- `GET /api/products` - Получить все товары
- `GET /api/products/search?q=...` - Поиск товаров
- `GET /api/products/category/:id` - Товары по категории
- `GET /api/products/manufacturer/:id` - Товары по производителю
- `GET /api/products/stock/:bool` - Фильтр по наличию
- `GET /api/products/sort/price/asc|desc` - Сортировка по цене
- `GET /api/products/sort/name` - Сортировка по названию
- `GET /api/products/filter/attribute?attr=X&value=Y` - Фильтр по атрибутам
- `GET /api/products/filter/numeric?attr=X&value=Y` - Числовой фильтр

### Заказы
- `POST /api/orders` - Создание заказа

## 🎵 Особенности системы

### Система скидок
1. **Комплект "Стартовый"**: 5% скидка при покупке гитары + усилителя + кабеля
2. **Запас струн**: 10% скидка на струны при покупке гитары

### Фильтрация товаров
- По категориям (электрогитары, акустика, усилители, эффекты, струны, аксессуары)
- По производителям (Yamaha, Fender, Ibanez, Marshall и др.)
- По наличию на складе
- По техническим характеристикам (цена, материал, мощность и т.д.)
- Поиск по названию

### Безопасность
- Хеширование паролей (MD5)
- Валидация входных данных
- Защищенные API эндпоинты

## 📁 Структура проекта

```
filp/
├── app/Main.hs              # Точка входа бэкенда
├── src/
│   ├── Models/              # Модели данных
│   ├── Logic/               # Бизнес-логика
│   ├── Database/            # Работа с БД
│   └── Routes.hs            # API маршруты
├── migrations/              # Миграции БД
├── data/seed.sql           # Начальные данные
└── frontend/                # React приложение
    ├── src/
    │   ├── components/      # React компоненты
    │   ├── services/        # API клиент
    │   └── App.jsx          # Главный компонент
    └── public/
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей фичи (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 📞 Контакты

- **Автор**: Kirill Vasilev
- **Email**: kirill.vasilev04@mail.ru
